## Examples

Some examples use native ESM imports and import maps, including
`20_timeline.html` and `21_timeline_slider.html`. Serve the repository over HTTP
when running them locally, for example:

```sh
npm run examples
```
