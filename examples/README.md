## Examples

Some examples use native ESM imports and import maps, including
`20_timeline.html`, `21_timeline_slider.html`, and `22_timeline_repeat.html`. Serve the repository over HTTP
when running them locally, for example:

```sh
npm run examples
```
